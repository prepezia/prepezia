
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to read, write, and list their own folder in `users/{userId}`
    match /users/{userId}/{allPaths=**} {
      allow read, write, list: if request.auth != null && request.auth.uid == userId;
    }
  }
}
