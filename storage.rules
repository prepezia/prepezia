
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Past questions folder - Requires Authentication
    match /past_questions/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && (
        request.auth.uid == 'ppuEIMHQI4MJLYdt2QLDrupho1E2' || 
        request.auth.token.email == 'nextinnovationafrica@gmail.com'
      );
    }

    // User-Specific Files - Requires Authentication
    match /users/{userId}/{allPaths=**} {
      allow read: if request.auth != null && (
        request.auth.uid == userId || 
        request.auth.uid == 'ppuEIMHQI4MJLYdt2QLDrupho1E2'
      );
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Public Files - Open to everyone (Logos, Favicon, etc.)
    match /public/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
