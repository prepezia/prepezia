
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to read, write, list and delete their own folder in `users/{userId}`
    match /users/{userId}/{allPaths=**} {
      allow list, read, write, delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
